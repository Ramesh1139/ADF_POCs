DROP TYPE TAB_TYPE_REIM_ATTR;

CREATE OR REPLACE TYPE REC_INPUT_STAGE AS OBJECT(
   EXT_DOC_ID             VARCHAR2(150), --changed the length	
   DOC_DATE               VARCHAR2(30),
   TYPE                   VARCHAR2(30),
   VENDOR_TYPE            VARCHAR2(30),
   VENDOR                 VARCHAR2(30),
   EXT_DOC_ID_2           VARCHAR2(150), --changed the length
   DOC_DATE_2             VARCHAR2(30),
   VENDOR_TYPE_2          VARCHAR2(30),
   VENDOR_2               VARCHAR2(30),
   VENDOR_NAME_2          VARCHAR2(240),
   SUPPLIER               VARCHAR2(30), -- New
   ORDER_NO               VARCHAR2(30),
   CURRENCY_CODE          VARCHAR2(30),
   TERMS                  VARCHAR2(30),
   LOCATION               VARCHAR2(30),
   -- CUSTOM_DOC_REF1        VARCHAR2(90), --Removed
   -- CUSTOM_DOC_REF2        VARCHAR2(90), --Removed
   -- CUSTOM_DOC_REF3        VARCHAR2(90), --Removed
   -- CUSTOM_DOC_REF4        VARCHAR2(90), --Removed
   SCAN_NO                VARCHAR2(30), --New
   SCAN_DATE              VARCHAR2(30), --New
   ORACLE_REASON_CODE     VARCHAR2(30), --New
   GL_DESC                VARCHAR2(360),--New
   SHIPMENT_REF_NO        VARCHAR2(30), --New
   WIRE_TRANSFER_NO       VARCHAR2(30), --New
   BANK_EXCHANGE_RATE     VARCHAR2(30), --New
   COST_ALLOCATION        VARCHAR2(30), --New
   DOC_CODE               VARCHAR2(30), --New
   DEPT                   VARCHAR2(30), --New
   SUPPLIER_SITE_ID       VARCHAR2(30), 
   DATE_FROM              VARCHAR2(30), --New
   DATE_TO                VARCHAR2(30), --New
   AUTH_NO                VARCHAR2(30), --New
   NOAP                   VARCHAR2(30), --New
   RTV_NUMBER             VARCHAR2(30), --New
   TOTAL_NONMERCH_COST    VARCHAR2(30), --Renamed From TOTAL_MERCH_COST
   EXTENDED_COST          VARCHAR2(30),
   TOTAL_COST_EXCL_TAX    VARCHAR2(30),
   TAX_AMOUNT             VARCHAR2(30),
   TOTAL_QTY              VARCHAR2(30),
   ALLOWANCE_AMT_HDR      VARCHAR2(30),
   ALLOWANCE_PERC_HDR     VARCHAR2(30),
   PREPAID_FLAG           VARCHAR2(30), --Renamed From MANUALLY_PAID_IND
   TEXT_I                 VARCHAR2(2000),
   TEXT_E                 VARCHAR2(2000),
   ITEM                   VARCHAR2(30),
   ITEM_DESCRIPTION       VARCHAR2(30),
   VPN_NUMBER             VARCHAR2(30),
   VPN_DESCRIPTION        VARCHAR2(120),
   INVOICE_QTY            VARCHAR2(30),
   UNIT_COST              VARCHAR2(30),
   REASON_CODE            VARCHAR2(30),
   ALLOWANCE_AMT_DET      VARCHAR2(30),
   ALLOWANCE_PERC_DET     VARCHAR2(30),
   INVOICE_QTY_DET        VARCHAR2(30),
   DUTY                   VARCHAR2(30),
   UNIT_OF_MEASURE        VARCHAR2(30),
   COST_COMPONENT1        VARCHAR2(30),
   COST_AMOUNT1           VARCHAR2(30),
   COST_COMPONENT2        VARCHAR2(30),
   COST_AMOUNT2           VARCHAR2(30),
   COST_COMPONENT3        VARCHAR2(30),
   COST_AMOUNT3           VARCHAR2(30),
   COST_COMPONENT4        VARCHAR2(30),
   COST_AMOUNT4           VARCHAR2(30),
   COST_COMPONENT5        VARCHAR2(30),
   COST_AMOUNT5           VARCHAR2(30),
   COST_COMPONENT6        VARCHAR2(30),
   COST_AMOUNT6           VARCHAR2(30),
   COST_COMPONENT7        VARCHAR2(30),
   COST_AMOUNT7           VARCHAR2(30),
   COST_COMPONENT8        VARCHAR2(30),
   COST_AMOUNT8           VARCHAR2(30),
   COST_COMPONENT9        VARCHAR2(30),
   COST_AMOUNT9           VARCHAR2(30),
   COST_COMPONENT10       VARCHAR2(30),
   COST_AMOUNT10          VARCHAR2(30),
   ENTRY_IP               VARCHAR2(30),
   EXT_DOC_ID_IP          VARCHAR2(150), --changed the length
   BROKER_FILE_IP         VARCHAR2(40),
   INVOICE_DATE_IP        VARCHAR2(30),
   MANUF_NAME_IP          VARCHAR2(30),
   MANUF_COMM_INV_IP      VARCHAR2(30),
   MANUF_COMM_INV_VAL_IP  VARCHAR2(30),
   MANUF_COMM_INV_PO_IP   VARCHAR2(30)
);

CREATE OR REPLACE TYPE SCH_MST.TAB_TYPE_REIM_ATTR AS TABLE OF REC_INPUT_STAGE;

CREATE OR REPLACE PUBLIC SYNONYM TAB_TYPE_REIM_ATTR
FOR SCH_MST.TAB_TYPE_REIM_ATTR;
/